export const DEFAULT_ERPNEXT_URL = 'https://erpnext.kodatechnologies.co.tz';

export const STORAGE_KEYS = {
  baseUrl: 'ako.baseUrl',
  authMode: 'ako.authMode',
  apiKey: 'ako.apiKey',
  apiSecret: 'ako.apiSecret',
  sessionCookie: 'ako.sid',
  user: 'ako.user',
  settings: 'ako.settings',
  bootstrap: 'ako.bootstrap',
  offlineQueue: 'ako.offlineQueue',
  lastWarehouse: 'ako.lastWarehouse',
  lastCompany: 'ako.lastCompany',
  rememberMe: 'ako.rememberMe',
  savedUsr: 'ako.savedUsr',
} as const;

export const APP_NAME = 'AKO Stock Take';
